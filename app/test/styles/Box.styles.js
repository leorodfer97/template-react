import {StyleSheet} from 'react-native';

export const BoxStyles = StyleSheet.create({
    box: {
        backgroundColor: '#0F0F0F',
        padding: 5,
        display: 'flex',
        width: '100%',
    },
    textBox: {
        color: '#FAFAFA',
        fontWeight: 'bold',
        fontSize: 20,
    }
});